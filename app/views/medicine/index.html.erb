				<div class="tabs">
					<div id="tab-1" class="tab">
						<article>
							<div class="text-section">
								<h1><p id="notice"><%= notice %></p></h1>
							</div>
							
						<ul class="states" >
							<%= form_tag '', :method => 'get' do %>
							<p>
							<%= text_field_tag :search_key, nil, placeholder: '请输入药品姓名...', size: 30 %>
							<%= submit_tag "Search", :name => nil %>
							</p>
							<% end %>
						</ul>
							<ul class="states" >
								<h1>药品列表</h1>
								<table>
								  <thead>
								    <tr style="text-align: center; COLOR: #0076C8; BACKGROUND-COLOR: #F4FAFF; font-weight: bold">
										<th width='10%'>序号</th>
										<th width='10%'>药品编号</th>
								      	<th width='20%'>药品名称</th>
								      	<th width='30%'>药品描述</th>
								      	<th width='30%' colspan="3">操作</th>
								    </tr>
								  </thead>

								  <tbody>
									  <% if @medicine != nil %>
								    <% @medicine.each_with_index do |medicine,i| %>
								      <tr>
										  <td align='center'><%= i+1 %></td>
										  <td align='center'><%= medicine.mid %></td>
								        <td align='center'><%= medicine.name %></td>
								        <td align='center'><%= medicine.description %></td>
								        <td align='center'><%= link_to 'Show', medicine %></td>
								        <td align='center'><%= link_to 'Edit', edit_medicine_path(medicine) %></td>
								        <td align='center'><%= link_to 'Destroy', medicine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
								      </tr>
								    <% end %>
									<% end %>
								  </tbody>
								</table>
							</br>
							
  每页显示<b><%= @medicine.per_page %></b>条数据,共有<b><%= @medicine.total_pages %></b>页,  共有<b><%= @medicine.total_entries %></b>条数据</br>
  <%= will_paginate @medicine, :container => false %>
								<br>

								<%= link_to '新增药品', 'medicine/new' %>
								
							</ul>

						</article>
					</div>
				</div>
